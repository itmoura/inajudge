#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const yargs = require("yargs");
const FuseFileExecution_1 = require("./FuseFileExecution");
const SYSTEM_ARGS = ["boostrap", "snippet", "run"];
function extractParams(args) {
    const primaryArgs = args._;
    const firstPrimaryArg = primaryArgs[0];
    return {
        primary: firstPrimaryArg,
        args: args,
        isSystemArgument: () => SYSTEM_ARGS.indexOf(firstPrimaryArg) > -1
    };
}
function initCLI() {
    const args = extractParams(yargs.args);
    if (!args.primary || !args.isSystemArgument() && FuseFileExecution_1.FuseFileExecution.test()) {
        return FuseFileExecution_1.FuseFileExecution.init(args);
    }
}
initCLI();
